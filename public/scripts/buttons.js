let scrollAction = function(elementId) {
  let minPixels = 16;
  let maxPixels = 30;
  let targetLoc = document.getElementById('issue_select');
  let scrollContainer = targetLoc;
}
let startButton = document.getElementById('start_button');

startButton.addEventListener("click", )
